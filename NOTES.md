# 專案交接筆記

> 這份文件是給「接手的人」或「換帳號後重新開始對話的自己」看的。
> 目的是不用重講一遍背景，看完就能接著做。
> 最後更新：2026-09-04

---

## 這個 repo 裡有什麼

```
yfs/
├── index.html          營運 SOP 檢索網站（單一檔案，含全部資料與程式）
├── config.js           Google 試算表與 Apps Script 的網址設定
├── 404.html            GitHub Pages 的路由 fallback（/workflow 重新整理用）
├── img/                SOP 操作截圖 521 張（471 webp + 50 png）
├── sop.image.png       網站左上角 logo
└── coupang-oms/        酷澎訂單管理系統（本地端 Flask 應用，與網站無關）
```

兩個東西是**各自獨立**的專案，只是放在同一個 repo 裡：

- `index.html` → 線上的 SOP 查詢網站
- `coupang-oms/` → 在自己電腦上跑的訂單處理工具

---

## 一、營運 SOP 檢索網站

### 現況

- 線上網址：<https://cyt0925.github.io/yfs/>
- 由 **GitHub Pages 從 `main` 分支**部署。改動要進 `main` 才會上線，
  推到其他分支網站不會變（這件事踩過一次坑）。
- 目前收錄 **46 份 SOP、305 個操作步驟、521 張截圖**
- 品牌：MARS、PG、CPG、跨品類、通用
- 通路：酷澎、MOMO、PCHOME、蝦皮、雅虎、東森、博客來、UberEats、遠時、HCT新竹物流

### 架構要點

**單一 HTML 檔。** 所有 CSS、JS、資料都在 `index.html` 裡（約 31 萬字元）。
沒有 build 步驟，改完直接推就好。

**資料有兩個來源，會互相覆蓋：**

1. `index.html` 裡的 `const SEED = [...]`（第 768 行附近）——內建的靜態資料，
   離線也看得到
2. `config.js` 指向的 Google 試算表 CSV——網站載入後會抓試算表，抓到就覆蓋 SEED

所以**日常更新內容是改試算表，不是改 HTML**。試算表有兩個分頁：
`SOPs`（文件層級）與 `Steps`（步驟層級），網址都在 `config.js`。

**編輯模式。** 網站右上角有「🔑 編輯模式」，輸入編輯碼後可以線上新增／修改 SOP，
寫回 Google 試算表。編輯碼**不在這個 repo 裡**（repo 是公開的），
設定在 Apps Script 的 `Code.gs` 裡的 `EDIT_CODE` 變數。

### 兩個檢視

**SOP 檢索**（預設頁）——關鍵字搜尋、依品牌／通路／出貨模式／物流方式篩選、
卡片與列表兩種排版、深淺色切換。

**流程圖**——這是網站最有價值的部分。27 份「通路出貨」SOP 共用同一條主幹，
歸納成 8 個階段（定義在 `index.html` 的 `TRUNK` 常數）：

| 代碼 | 階段 | 說明 |
|---|---|---|
| t1 | 接單與確認 | 確認到貨日、數量、料號 |
| t2 | 建立資料夾與文件 | 製作採購單、上傳表等 |
| t3 | 系統下採／拋單 | EIP 申請或通路系統拋單 |
| t4 | 通知窗口 | 發信給採購、原廠、業務 |
| t5 | 竹運倉核對 | 只有走竹運的文件才有 |
| t6 | 製作嘜頭／標籤 | 只有需要嘜頭的通路才有 |
| t7 | 出貨文件確認 | 採購單、驗收單、入庫憑證 |
| t8 | 發送與登記 | 發信＋雲表登記 |

每個步驟可以標註對應階段（`Steps` 分頁的 tag 欄，例如 `t1` 或 `t2,t7`），
標成 `exception` 表示是缺貨下修、拆併單這類例外情境，不屬於 8 步主幹。

畫面上可以看出**哪份 SOP 缺了哪個階段**——缺口會顯示成空的。
導覽列的「流程圖」旁邊如果出現徽章，代表有步驟還沒分類。

### 已知待補

- 部分通路尚未納入
- 有些步驟只有截圖、缺文字說明（匯出給 AI 用的時候這些會變成空白）

---

## 二、酷澎訂單管理系統（`coupang-oms/`）

本地端跑的 Flask 應用，不是網站的一部分。詳細說明看 `coupang-oms/README.md`，
那份寫得很完整，**接手前一定要讀**，尤其是「核心防呆」那張表。

### 一句話說明

把酷澎整合表整份丟進去，系統自動去重、抓出酷澎偷改過的單、記錄誰在什麼時候
改了什麼，最後一鍵匯出乾淨的 Excel 給後端。

### 最容易踩雷的三件事

1. **唯一鍵必須是 `(po_number, sku_id)` 複合鍵。**
   一張 PO 最多有 29 個 SKU，把 `po_number` 單獨設 UNIQUE 會靜悄悄丟掉 8 成資料。
2. **`warehouse` 和 `delivery_date` 不能進主鍵。**
   這兩個欄位天天在改，進主鍵會讓酷澎改倉時被判定成新單，產生數量翻倍的幽靈列。
3. **資料不在 `coupang-oms/` 裡面，在它隔壁的「資料與設定」資料夾。**
   更新程式時整包覆蓋沒關係；要備份的是「資料與設定」。

### 驗證

```bash
cd coupang-oms
python test_flow.py     # 用真實整合表跑完整流程，46 項檢查
```

---

## 三、進行中／待決定的事

### AI 問答功能（等主管核准）

想做的事：讓 SOP 網站可以直接回答問題，而不只是查文件。

**已完成的驗證：** 用 NotebookLM 拿 5 個實際作業問題測過，**4 題答對**。
代表這批資料的結構與品質足以支撐 AI 問答，不需要重新整理內容。

**規劃的做法（重要）：** 不要把 46 份 SOP 全部送給 AI，那樣每題成本會高 10 倍。
正確做法是**先用網站現有的搜尋找出相關的 2–3 份 SOP，只送這幾份**。
資料結構與搜尋這兩塊最難的部分已經做好了，缺的只是中間的串接層。

**成本概估：** 依使用量與模型選擇，每月約 NT$105–1,760。

**API 金鑰放哪：** 沿用現在編輯碼的做法——放在 Apps Script 裡，
不要放進這個 repo（repo 是公開的）。

**卡在哪：** 需要主管確認兩件事——
1. 開一個 API 帳號並綁定付款方式
2. 釐清「SOP 目前是公開連結」這件事要不要處理

有一支 `/tmp/export_ai.js` 的腳本可以把 46 份 SOP 匯出成單一純文字檔（給 AI 吃的格式），
但它在暫存目錄、沒有進 repo，需要的話重寫一份即可（邏輯不複雜，讀 SEED 逐份輸出）。

### 簡報

主管要在月會報告 SOP 網站與酷澎系統。已經討論過的敘事結構：

```
我的觀察 → 因此我做了什麼
```

觀察的三個層次：
1. 營運工作看似固定，但每人負責線別／品牌不同、做事方式也不同，
   流程變成個人經驗 → 休假、代理、輪調時接不上
2. 新人沒有系統化訓練只能問人，而被問的是最忙的人 →
   壓縮晨間轉單、拋單這類高時效作業
3. 公司試過把流程寫成 Word 統一放資料夾，但檔案靜態零散、格式不一、
   看久疲勞 → 流於形式，大家還是口頭問

轉折句：**問題不在「有沒有文件」，而在「文件能不能真的被使用」。**

然後才講網站做了什麼：集中 → 查得到 → 比得出（8 階段主幹）→ 改得動（試算表＋編輯碼）。

「每天在數十個 Excel 檔人工剪貼比對」這個痛點**不要放開場**——
網站解決不了它，講了會讓聽眾期待落空。放到最後當「下一步」的伏筆比較好。

---

## 四、開發時要注意的

**分支。** 開發在 `claude/sop-search-website-3uq026`，
但**要上線必須合併進 `main`**，因為 GitHub Pages 只看 main。

**本地預覽。**

```bash
python3 -m http.server 8791
# 然後開 http://localhost:8791/index.html
```

不能直接用 `file://` 開，Google 試算表的 CSV 抓取會被 CORS 擋掉。

**測試。** 這個專案沒有自動化測試框架。改完 `index.html` 建議用 Playwright
（系統已裝在 `/opt/node22/lib/node_modules/playwright`，
Chromium 在 `/opt/pw-browsers/chromium`）開起來實際點過一遍，
確認搜尋、篩選、流程圖切換都正常。

**不要放進 repo 的東西：** 編輯碼、API 金鑰、任何密碼。這個 repo 是公開的。
