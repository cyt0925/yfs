# 交接筆記

寫給「接手這個 repo 的下一個人（或下一段對話）」看的。
目的是讓完全沒有前後文的人，看完這一份就知道：這裡有什麼、跑在哪、
現在做到哪、哪些地雷已經踩過不要再踩。

技術架構與操作說明不重複寫在這裡，那些在 `coupang-oms/README.md`。
這份只記「狀態」與「經驗」。

最後更新：2026-09-15

---

## 1. 這個 repo 裡有三個東西

| 資產 | 位置 | 說明 |
|---|---|---|
| **酷澎訂單管理系統** | `coupang-oms/` | 主系統。Flask + PostgreSQL，跑在 Render 上 |
| **採購表轉換** | `coupang-oms/purchase.py`＋`purchase_templates/` | 跟訂單管理**完全獨立**，不共用任何資料表。網址 `/purchase` |
| **商品主檔自動化** | `coupang-oms/master/`（套件）＋`templates/master.html`＋`static/master.js` | 取代 OP「彙總表 → 專案報價檔 → 樞紐 → 總表」的手工流程。表全部 `mst_` 前綴，跟訂單管理分開。網址 `/master`。**設計筆記在 `coupang-oms/docs/商品主檔自動化_設計筆記.md`** |
| **SOP 檢索網站** | repo 根目錄的 `index.html`、`config.js`、`img/` | 純靜態網頁，內容存在 Google 試算表，走 GitHub Pages |

---

## 2. 分支：這件事最容易搞錯

| 分支 | 用途 |
|---|---|
| `claude/coupang-order-system-design-n17g7z` | **正式站的部署來源**。Render 接的是這個分支，push 上去就會自動部署 |
| `main` | 參考用的完整快照。**不是部署來源** |
| `claude/sop-search-website-3uq026` | SOP 檢索網站那條線的開發分支 |

**歷史教訓**：`main` 曾經停在 2026-08-17 的殘缺版本長達兩週，缺了驗收單簽名、
採購表轉換、`static/` 整個目錄。任何人（或任何 AI）預設讀 `main` 都會得到
「這些功能不存在」的錯誤結論，實際上只是分支沒跟上。2026-09-03 已經把開發
分支合併回 `main` 補齊，之後請記得定期合併回去，不要讓它再落後。

---

## 3. 目前狀態（2026-09-03）

**版本號 `2026-09-03.1`**（`coupang-oms/app.py` 最上面的 `BUILD_VERSION`，
畫面右下角也會顯示，用來確認正式站到底跑到哪一版）

### ✅ 部署已確認成功（2026-09-04）

`58c56a7`（修正部署失敗的 commit）那次部署是**成功的**。2026-09-04 用
`/healthz` 驗過，回 `{"build_version":"2026-09-03.1","status":"ok"}`
——版本號對得上，且 `status: ok` 代表資料庫也連得上。

**以後要確認部署狀態，打 `正式站網址/healthz` 就好**，不用登入、不用進
Render 後台。它除了回版本號，還會真的連一次資料庫才回 `ok`，所以一次
就能分辨「行程活著但資料庫掛了」這種故障（見 `app.py` 的 `healthz()` 註解）。
版本號對不上就代表那次部署沒生效或失敗，這時才需要去 Render 後台看 Logs。

### 最近完成的功能

- 驗收同步一併收「驗收金額」（酷澎後台實收金額稅後，照實際收貨數量算；腳本 7.5 起，之前抓的是下單金額會對不上下修過的單）：首頁列表與 PO 總表各多一欄。
  酷澎後台那支 Tampermonkey 腳本現在放在 `coupang-oms/tools/coupang_po_verify.user.js`，
  後端改欄位時要一起改、並請使用者把整份貼回 Tampermonkey（部署不會自動更新它）。
  金額欄位名稱在酷澎 API 裡是猜的（候選清單見腳本 `AMOUNT_FIELDS`），抓不到會在
  面板上警告並把欄位名印到 Console，拿到真實名稱後補進清單即可。
- 首頁到貨日／明細交期改用同一套取值；酷澎只改部分品項時兩邊都會標「各品項不一致」
- 紙潔採購表 G1 改回活的公式 `="採購數量"&SUBTOTAL(...)`，並同時寫入快取值
- 採購表「匯出所選（各自一個檔）」改成直接逐張下載，不包 zip；檔名維持不帶單號（使用者明確要求）
- CPG 線別新單匯入時自動帶配送方式「原廠(EM)」，並補齊既有舊資料
- 驗收單簽名 SOP 從獨立按鈕改成簽名視窗裡的分頁籤（排在「位置校正」右邊）
- 採購表批次匯出：同一張 PO 時 zip 檔名直接用單號
- 瑪氏採購表：匯出檔名沿用匯入檔名、可跨資料夾累加選檔
- `/healthz` 健康檢查端點
- `coupang-oms/deploy/nginx-oms.conf`：未來遷移到 GCP 時要用的反向代理設定，**還沒實際套用過**

---

## 4. 踩過的坑（重要，不要再踩一次）

### 🔴 PostgreSQL 模式下，SQL 字串裡不能有裸的 `%`

**這個坑讓正式站連續兩次部署失敗，而且本機測試完全測不出來。**

`db.py` 的 `_PgCursor.execute()` 預設 `params=()`（不是 `None`），只要不是 None，
psycopg2 就會把整段 SQL 當成「可能帶 `%s` 佔位符號」去解析。所以只要 SQL 文字
裡出現裸的 `%`（例如 `LIKE 'CPG-%'`），就會被誤認成佔位符號、抓不到對應參數，
啟動時直接炸成 `IndexError: tuple index out of range`。

**正確寫法**：LIKE 的樣式一定要綁參數傳進去

```python
conn.execute("... WHERE line LIKE ?", ("CPG-%",))   # ✅ 值裡面有 % 沒關係
conn.execute("... WHERE line LIKE 'CPG-%'")         # ❌ SQL 文字裡有 % 會炸
```

### 🔴 本機測試環境跟正式站用不同資料庫

本機（沒設 `DATABASE_URL`）走 SQLite，正式站走 PostgreSQL。
**兩者行為有差異，本機測試全過不代表正式站不會炸**（上面那個坑就是活生生的例子）。
凡是改到 `db.py` 或任何 SQL，都要特別想一下「這在 Postgres 上會怎樣」。

### 🟡 改了 Tailwind class 一定要重新編譯 CSS

`templates/*.html` 裡新用到的 Tailwind class，如果沒重新編譯 `static/tailwind.css`，
畫面上會**完全沒有效果，而且不會報錯**，非常容易誤判成「我的 class 寫錯了」。

```bash
cd coupang-oms/.build-tools
npx tailwindcss -i input.css -o ../static/tailwind.css --config tailwind.config.js --minify
```

### 🟡 一張 PO 有多個 SKU

唯一鍵是 `UNIQUE(po_number, sku_id)`，**不能**把 `po_number` 設成 UNIQUE。
真實整合表裡一張 PO 最多有 29 個 SKU，設錯會靜悄悄丟掉 80% 的資料。

### 🟡 每次改完記得升 `BUILD_VERSION`

不升的話，就沒辦法用「畫面右下角版本號」確認正式站到底部署生效了沒有。

---

### 🟡 同事口中的「file」多半是「資料夾」，先確認再動手

2026-09-16 採購表轉換：Chloe 說「產出自動拆 file、依 PO 單命名」，我先照「檔案」
理解，把瑪氏同一張 PO 的兩份通知單合成一個檔，結果 Jerry 說「資料夾在哪」——
他們要的是 zip 解開後**一張 PO 一個資料夾**，同一張 PO 的幾份各自一個檔放在同一個
資料夾裡。合併那版當天就拆掉了（測試【5.2b】現在驗的是資料夾版）。
以後聽到「分 file／拆 file／放同一個 file」先問清楚是檔案還是資料夾。
資料夾版上線後 Chloe 又說 P&G 不要資料夾（一張 PO 一個檔、檔名帶單號，多一層礙事），
所以只有瑪氏分資料夾（`LINES` 的 `zip_po_folders`），P&G／紙潔的 zip 打開就是檔案。

## 5. 測試

```bash
cd coupang-oms
python3 test_flow.py       # 訂單管理主流程，187 項
python3 test_purchase.py   # 採購表轉換，60 項
```

兩支都必須全過才能推。用的是 `samples/` 裡的真實資料跑端到端，
不是 mock，所以過了代表流程真的通。

---

## 6. 進行中、還沒完成的事

### 遷移到主管的 GCP 雲端戰情室

規劃書已經寫好（Markdown 版在對話裡給過使用者，內容也整理進
`coupang-oms/deploy/nginx-oms.conf` 的註解裡）。

**卡在等主管回覆八個問題**，沒有答案之前不要開工，主要是：
主機規格與可用記憶體、戰情室用什麼技術跑在哪個埠、目前怎麼連進戰情室、
戰情室的登入機制、爬蟲排程時段與記憶體用量、可接受的施工時段、
資料庫要不要遷 Cloud SQL（誰付錢）、主機上 AI 工具用誰的帳號。

已經先做好、不用等決策的部分：`/healthz`、nginx 設定片段、路徑前綴支援
（`ProxyFix`，掛在別人網址子路徑下時才用得到，目前在 Render 上是無作用的）。

**最高風險**：目標主機是主管正在營運的正式機，上面跑著吃記憶體的爬蟲。
部署時第一件事就是替我方服務設記憶體與 CPU 上限，避免拖垮戰情室。

### 試用期簡報

使用者在另一個對話做簡報。簡報用的素材（logo 縮放循環影片，SOP 站版與
訂單系統版各一支）已經產出並交付，**沒有存進 repo**，在使用者自己電腦裡。

---

## 6b. 商品主檔自動化：2026-09-11～09-15 這一輪的狀態

**做到哪**：三步都能用、同事在正式站試用中（版本 `2026-09-15.2x`）。① 匯酷澎主檔（一個線別一份）
與寶僑總表；② 匯訂單彙總表（可多檔、可分次加）、行事曆、篩選列、就地編輯、PO 改期、倉別箱數、
各次匯入的變動（新增／有變＋對帳）；③ 總表現算、匯出總表照寶僑總表底稿填（缺的日期自動插欄）、
匯出專案報價檔（可跨月、可只匯某次變動）。清除資料（管理員）。

**這輪學到的、下次別再犯**：
- 同事是 OP 不是工程師：要口語、不要問基本問題、UI 一個東西講一次就好（曾被罵三個元件講同一件事）。
- 需求是邊做邊長的，一天推了 20 幾版。做過又拆掉的東西列在設計筆記第四節，**別做回來**。
- 「以出貨數量為主、箱數不湊整」「總表沒有線別」「總表只吃箱數、不混進總表沒有的商品」
  「匯出要跟寶僑總表一模一樣含順序」是同事／Chloe 明講的，不要自己加聰明。
- 兩個真的漏掉的 bug 都在前端（誤刪 loadLogs、下拉選回第一條沒反應）→ 現在有 `test_master_ui.py`。
- 中文錯字：「匯」「驟」曾被打成 Big5 裡沒有的怪字，同事看不懂。repo 有 `coupang-oms/check_chinese.py` 掃這種字（CI 會跑）；對話訊息送出前自己再看一次。

**技術債清理（09-15 下午做完）**：test_flow 在 PostgreSQL 下那 2 個「設定檔」失敗修掉了（正式站模式設定存 app_settings 表不是檔案，測試改成兩種模式各驗各的），CI 三套現在 SQLite／PostgreSQL 都全跑；CI（`.github/workflows/tests.yml`：SQLite／PostgreSQL／Playwright 三組）、
master.py 1724 行拆成 `master/` 八個檔、前端 JS 抽成 `static/master.js`、兩支長函式拆小、死碼清掉、
總表不同長相的測試案例、設計筆記。app.py（2816 行）與 index.html（1888 行 JS）**沒動**，那是訂單管理
的範圍，要拆另外排。

**2026-09-22 改單原因**：主管要統計「一年改了幾次單、為什麼、算成本」。第一步先把原因記起來：人手改出貨數量／
交貨日一定要選缺貨／沒車／酷澎要求／其他（存 `mst_logs.reason`），越早開始記越有價值。統計頁與匯出**還沒做**，
同一天下午做了 **④ 改單統計** 分頁（`master/stats.py`）：從／到月份＋線別，四張卡（PO 張數、被改過的 PO 與佔比、改單次數
分酷澎改／我們改、估計工時）、每月一列、原因分佈、改最多次的 PO、匯出 Excel。「每次改單約幾分鐘」人填、留 0 不算。
之後又加了：數字都能點開看明細（一次改單一列）、「改了什麼」分改數量（下修／上修）／改交期／品項被拿掉、
Excel 多「改了什麼」「明細」兩個分頁。原則是主管要的都放統計頁，同事輸入端不再多任何一步。
測試資料：Jerry 不開測試站，改成原因視窗多一個「這是測試，不算進統計」勾選框（統計預設排除、可勾「含測試」看），
匯入試看就只按預覽。給同事的規矩：正式站試改單勾測試、試匯入只按預覽。

**2026-09-22 總表搬進系統・第一步（欄位與匯入）**：Jerry 的最終目標是把寶僑總表整份搬進介面、Excel 只剩匯出。
先把總表一百欄逐欄判讀（`coupang-oms/docs/寶僑總表_欄位與公式對照.md`），發現它靠四份外部檔 VLOOKUP，
其中庫存三欄公式指歪、sell-thru 表已失效。這輪做了：主檔加 GIV／NIV、每月供需與進銷表、同一匯入口認
supply 表／Coupang Master／庫存銷售表、庫存與到月底預估照 Excel 公式現算、「上次上傳」四格。
**不做 Google 試算表連動**（兩個正本會互蓋），之後若業務仍用試算表，做「比對後勾選接受」不做自動覆蓋。
還在等：酷澎銷售報表樣本、Chloe 手填三欄的來源、4 月起算的確認、瑪氏拆單（Alice 的規格已分析，見對話）。

**2026-09-23 總表搬進系統・第二步（③ 總表看板）**：③ 分頁從「只有每日出貨寬表」改成看板（`master/board.py`＋
`master.js` 的 renderBoard）：四張卡、商品／品牌／每日出貨三個視圖、搜尋、品牌目標點格子就填（`mst_brand_targets`）。
每一格的公式對照總表寫在 board.py 檔頭與 `docs/寶僑總表_欄位與公式對照.md`。
**同日下午（第三步・匯出，`master/sheet_export.py`）**：匯出總表改成把 GIV／NIV、該月 Supply／demand、庫存三欄、品牌目標
也填回底稿（系統有值才填），附「系統看板」「系統填入說明」兩個分頁。**教訓**：上午推看板時匯出還沒對齊，畫面對、Excel 舊，
Jerry 很不高興（他最忌諱數值錯）——以後畫面跟匯出要一起做完、對過才推。順帶修了判讀錯誤：品牌目標是一季一個數字，不是一個月。

**2026-09-23 晚：在系統上操作**：③ 看板的 GIV／NIV／Supply／demand 可以點格子改；庫存的下單改成從訂單明細累加
（一個商品一個月看，系統有單就用系統的）；重匯舊總表會蓋到系統上改過或 supply 表／Coupang Master 更新過的數字時，
先列出來問「保留系統的／用總表的蓋過去」。細節在設計筆記第三節。跟主管講的口徑：系統不是讓業務不用更新，
而是把每個業務自己整理變成系統統一整理；對方給的數字（Supply、進價、實銷）拿到新檔還是要丟進來。
**還在等**：酷澎銷售報表樣本（實銷來源，To_PG_Weekly_Sales_by skus／PG Sale by SKU），Jerry 去問主管了。

**2026-09-23 瑪氏出貨（/mars，`coupang-oms/mars/`）**：Alice 的瑪氏五步先做了前兩步——拆單（產拆單表＋EIP 上傳用採購表）、
回填 EIP 採購單號與約倉時間。訂單不另外傳，用 ② 訂單明細裡的瑪氏訂單；瑪氏商品總表在那頁上傳。細節、真檔確認過的事、
還在等 Alice 的事都在 `coupang-oms/docs/瑪氏出貨_設計筆記.md`。**②匯入多存地址、報價備註，之前匯的瑪氏單要重匯一次**組出商品才對得到。
同日晚上補了第三步 **瑪氏採購單**（`mars/po.py`）：填了 EIP 單號與約倉時間的拆單表套 V2 範本，每列一顆「採購單」、也能整段打包。
各倉的電話、ship-to 要人在頁面下方「採購單設定」填一次（正式站還沒填，Jerry 手上的舊採購單有 TAO1 的），沒填那倉產不出來；
特殊需求文字、效期要求、假日都在那裡改，Alice 改規則不用改程式。

**2026-09-22 上方選單改版**：功能變多，上方那排不能再長。做了三頁共用的「線別工具」按鈕（`templates/_line_menu.html`，
點開不是滑過），分寶僑／瑪氏／紙潔三欄，各線別的工具往裡面加。三顆按鈕（`lm_mode='split'`）做過，Jerry 看了覺得擠，最後三頁都用一顆
「線別工具」：白底深字、前面三個線別色點、不放 logo；點開三欄各帶 logo 與 logo 取的色（寶僑藍、瑪氏藍青漸層、紙潔紅）；共用的（上傳整合表、驗收單簽名、採購表轉換、修改歷程、
匯出報表）留在原位不動。首頁原本的「商品主檔自動化」按鈕收進寶僑那欄；瑪氏、紙潔先「準備中」，瑪氏的新功能做好放進去。
Logo 是 Jerry 給的三個檔（`static/line_pg.png`／`line_mars.png`／`line_cpg.png`，縮到 72px 高）。
選單樣式全部寫在 partial 裡，不用 Tailwind class。
資料只從 9 月中系統上線後才有。算法見 stats.py 檔頭與設計筆記第三節。

**擱著的**：公槽同步（同事說等他開口，**不要主動提**）、匯入舊專案報價檔、匯出總表跨月、配額畫面。

**正式站部署**：Render 仍接 `claude/coupang-order-system-design-n17g7z`；開發分支
`claude/github-reading-setup-rkyfuf` 每次都 fast-forward 推到部署分支（兩條分支目前指同一個 commit）。
建議到 Render 把 Auto-Deploy 改成「等 CI 通過才部署」。

---

## 7. 給接手的下一段對話

最快進入狀況的順序：

1. 先讀 `coupang-oms/README.md`（架構、防呆規則、部署方式）
2. 再讀這一份（狀態、地雷）；動商品主檔模組前再讀 `coupang-oms/docs/商品主檔自動化_設計筆記.md`
3. `git log --oneline -20` 看最近做了什麼
4. 確認正式站版本號跟 `BUILD_VERSION` 對得起來

**要注意的環境事實**：正式站是雲端服務（Render），不是跑在誰的電腦上的本機程式。
不需要壓縮檔案、不需要覆蓋資料夾、不需要重開什麼 `.bat`——那些是本機開發模式
才有的流程。改完 `git push` 到部署分支就會自動上線。
